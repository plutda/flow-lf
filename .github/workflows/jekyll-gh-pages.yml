name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: npm install
        
      - name: Build
        run: npm run build

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.JOB }}
          PUBLISH_BRANCH: main
          PUBLISH_DIR: ./docs
          user_name: pluto # 不用改
          user_email: w_zb@foxmail.com # 不用改
          commit_message: 自动部署 # 5.部署时的 git 提交信息，自由填写
